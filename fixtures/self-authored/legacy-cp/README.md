# Minimal old CP fixture

Source: self-authored for `mod-dp-bridge` regression testing.
License: CC0-1.0 (`LICENSE.txt`).

This is a standalone old-style CP `PatchSet`, not a normal Mod directory. It
tests HJSON comments, quoted patch-operator keys, string booleans, item stacks,
and top-level item/block/unit sections.
